-- =============================================================================
-- ZEN Team Scheduling — one-shot setup for Supabase SQL Editor
-- =============================================================================
-- In Supabase Dashboard: SQL → New query → paste this ENTIRE file → Run
--
-- Or run locally: npm run db:setup  (requires DATABASE_URL in .env.local)
-- =============================================================================

-- Paste contents of migrations/20250515000000_initial_schema.sql below, then seed.sql
-- Tip: Run migration file first, then seed file, as two separate queries if preferred.
